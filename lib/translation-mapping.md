# Translation Mapping for Admin Pages

## Critical strings that need `t()` replacement:

### Events Page (/app/admin/events/page.tsx)
- "Events" → t("events.title")
- "Manage all events and guest transfers" → t("events.subtitle")
- "Add Event" → t("events.addEvent")
- "Search events..." → t("events.searchPlaceholder")
- "Event" → t("events.tableEvent")
- "Date & Time" → t("events.tableDate")
- "Location" → t("events.tableLocation")
- "Guests" → t("events.tableGuests")
- "Status" → t("events.tableStatus")
- "Actions" → t("events.tableActions")
- "Event Name" → t("events.eventNameLabel")
- "Date (DD/MM/YYYY)" → t("events.dateLabel")
- "Time (24h)" → t("events.timeLabel")
- "Event Address" → t("events.addressLabel")
- "Notes (Optional)" → t("events.notesOptional")
- "Create Event" → t("events.createButton")
- "Save Changes" → t("events.saveChanges")
- "Cancel" → t("common.cancel")
- "Choose on Map" → t("events.chooseOnMap")

### Drivers Page (/app/admin/drivers/page.tsx)
- "Drivers" → t("drivers.title")
- "Manage drivers and assign them to guest transfers" → t("drivers.subtitle")
- "Add Driver" → t("drivers.addDriver")
- "Search drivers..." → t("drivers.searchPlaceholder")
- "Available" → t("drivers.available")
- "Total Drivers" → t("drivers.totalDrivers")
- "Driver" → t("drivers.tableDriver")
- "Contact" → t("drivers.tableContact")
- "Vehicle" → t("drivers.tableVehicle")
- "Status" → t("drivers.tableStatus")
- "Actions" → t("drivers.tableActions")
- "Assign" → t("drivers.assign")
- "Assign Driver to Guest" → t("drivers.assignDialogTitle")
- "Select Guest" → t("drivers.selectGuest")
- "Assign Driver" → t("drivers.assignButton")
- "Add New Driver" → t("drivers.addDriverTitle")
- "Full Name" → t("drivers.fullName")
- "Email" → t("drivers.email")
- "Phone" → t("drivers.phoneLabel")
- "Vehicle Type" → t("drivers.vehicleType")
- "Vehicle Model" → t("drivers.vehicleModel")
- "License Plate" → t("drivers.licensePlate")

### Users Page (/app/admin/users/page.tsx)
- "Users" → t("users.title")
- "Manage user accounts, roles, and module access permissions" → t("users.subtitle")
- "Add User" → t("users.addUser")
- "Search users by name or email..." → t("users.searchPlaceholder")
- "Name" → t("users.tableName")
- "Email" → t("users.tableEmail")
- "Role" → t("users.tableRole")
- "Phone" → t("users.tablePhone")
- "Status" → t("users.tableStatus")
- "Actions" → t("users.tableActions")
- "Active" → t("users.active")
- "Disabled" → t("users.disabled")
- "Edit User" → t("users.editUser")
- "Change Password" → t("users.changePassword")
- "Delete User" → t("users.deleteUser")
- "Add New User" → t("users.addUserTitle")
- "Edit User" → t("users.editUserTitle")
- "Full Name" → t("users.fullName")
- "Module Access" → t("users.moduleAccess")
- "Dashboard" → t("users.moduleAccessDashboard")
- "Events" → t("users.moduleAccessEvents")
- "Drivers" → t("users.moduleAccessDrivers")
- "Reports" → t("users.moduleAccessReports")
- "Users" → t("users.moduleAccessUsers")
- "Create User" → t("users.createButton")
- "Creating..." → t("users.creating")
- "Save Changes" → t("users.saveChanges")
- "Saving..." → t("users.saving")
- "Change Password" → t("users.changePasswordTitle")
- "New Password" → t("users.newPassword")
- "Repeat New Password" → t("users.repeatPassword")
- "Delete User" → t("users.deleteUserTitle")
- "Cancel" → t("common.cancel")
- "Admin" → t("users.admin")
- "User" → t("users.user")

### Reports Page (/app/admin/reports/page.tsx)
- Already done (title and subtitle)
- Need to add remaining stats and labels
